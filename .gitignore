node_modules/
dist/
.DS_Store

# Local dev TLS certs (https dev server) — never commit
*.pem
cert.pem
key.pem

# Stray test media (downloaded MIDI samples)
*.mid
*.midi

# Saved project files (local scratch)
*.kkproj

# Legal drafts and notes (local only until lawyer-reviewed)
legal/
